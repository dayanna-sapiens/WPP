@{
    ViewBag.Title = "Control de Operaciones";
    Layout = "~/Views/Shared/_Layout.cshtml";



}

<div class="row" style="margin-top:20px; ">

    <div class="col-md-3"></div>
    <div class="col-md-6" style="padding-right:30px;">
        <br />
        <h3>Bitácora de Clientes</h3>
        <br />
        @using (Html.BeginForm("ClienteBitacora", "ControlOperaciones", FormMethod.Post, new { id = "frmCliente", @class = "form-horizontal" }))
        {

            <div class="form-group">
                <label class="col-md-2" style="padding-left:0px; padding-right:0px;">Nombre</label>
                <div class="col-md-10">
                    @Html.TextBox("Nombre", "", new { @class = "form-control", id = "Nombre" })
                </div>
            </div>
            <div class="form-group">
                <label class="col-md-2" style="padding-left:0px; padding-right:0px;">Número</label>
                <div class="col-md-4">
                    @Html.TextBox("Numero", "", new { @class = "form-control", id = "Numero", @type = "number", @min = "0", @max = "100000000000" })
                </div>
                <label class="col-md-2" style="padding-left:0px; padding-right:0px;">Tipo</label>
                <div class="col-md-4">
                    @Html.DropDownList("Tipo", new SelectList(ViewBag.ListaTipo, "Id", "Nombre"), "Todos", new { @class = "form-control", id = "Tipo" })
                </div>
            </div>
            <div class="col-12-md" style="float:right; padding-top:100px;">
                @*   <a id="btnReporteRecibos" class="btn btn-primary" disabled>Reporte de Recibos   <i class="fa fa-copy"></i></a>*@
                <a id="btnReporte" class="btn btn-primary">Reporte   <i class="fa fa-file-o"></i></a>
                <a id="btnExcel" class="btn btn-primary">Excel   <i class="fa fa-file-excel-o"></i></a>
                <button id="btnSubmit" type="submit" style="display:none;" class="btn btn-primary"></button>
                @Html.ActionLink("Cancelar", "ClienteBitacora", "ControlOperaciones", null, new { @Class = "btn btn-primary" })
            </div>
        }
    </div>

    <div class="col-md-3"></div>
</div>



<script language="javascript" type="text/javascript">

    $(document).ready(function () {


        jQuery.validator.methods.date = function (value, element) {
            var isChrome = /Chrome/.test(navigator.userAgent) && /Google Inc/.test(navigator.vendor);
            var isSafari = /Safari/.test(navigator.userAgent) && /Apple Computer/.test(navigator.vendor);
            if (isSafari || isChrome) {
                var d = new Date();
                return this.optional(element) || !/Invalid|NaN/.test(new Date(d.toLocaleDateString(value)));
            } else {
                return this.optional(element) || !/Invalid|NaN/.test(new Date(value));
            }
        };



    });// end document ready


    // Al dar click en el boton de buscar, obtiene los contratos que coincidan con el cliente o contrato ingresado
    $("#btnReporte").click(function () {

        var nombre = $("#Nombre").val();
        var numero = $("#Numero").val();
        var tipo = $("#Tipo").val() == "" ? "Todos" : $("#Tipo").val();
        var tipoReporte = $('input[name=rdbTipo]:checked').val();

        var formato = "pdf";
        window.open("/ControlOperaciones/ReporteClienteBitacora?nombre=" + nombre + "&numero=" + numero + "&tipo=" + tipo + "&tipoReporte=" + tipoReporte + "&formato=" + formato, 'mywindow', 'fullscreen=yes, scrollbars=auto');
    });

    // Al dar click en el boton de buscar, obtiene los contratos que coincidan con el cliente o contrato ingresado
    $("#btnExcel").click(function () {

        var nombre = $("#Nombre").val();
        var numero = $("#Numero").val();
        var tipo = $("#Tipo").val() == "" ? "Todos" : $("#Tipo").val();
        var tipoReporte = $('input[name=rdbTipo]:checked').val();

        var formato = "excel";
        window.location.assign("/ControlOperaciones/ReporteClienteBitacora?nombre=" + nombre + "&numero=" + numero + "&tipo=" + tipo + "&tipoReporte=" + tipoReporte + "&formato=" + formato);
    });



    // Este metodo se encarga de dar formato a la fecha para que se muestre en el listado
    function convertDate(inputFormat) {
        function pad(s) { return (s < 10) ? '0' + s : s; }
        var d = new Date(inputFormat);
        return [pad(d.getDate()), pad(d.getMonth() + 1), d.getFullYear()].join('/');
    }



</script>



